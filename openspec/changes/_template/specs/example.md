# Delta spec template — copy per capability into openspec/changes/{change-id}/specs/

## ADDED Requirements

### Requirement: Example capability

The system SHALL provide example behavior described in docs/requirements.md.

#### Scenario: Happy path

- **WHEN** user performs the primary action
- **THEN** the system responds with expected outcome
